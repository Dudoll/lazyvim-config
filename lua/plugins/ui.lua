return {
  {
    "snacks.nvim",
    opts = {
      -- Open directly into a normal empty buffer instead of showing the
      -- LazyVim logo and dashboard shortcut list.
      dashboard = { enabled = false },
    },
  },
}
